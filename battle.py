from dataclasses import dataclass


@dataclass
class Player:
    name: str
    elo: int
    wins: int
    losses: int

# set random seed
# generate 2k dataset w/ stable diff
    
# for num_players
# load a random player from .npy file in directory
# train eval player

# remove losing player
# duplicate winning player with variation (use LLM to generate variation)
# 

